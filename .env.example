# Copy to .env and fill in. Never commit .env.
# Required (fail loudly if missing):
TAVILY_API_KEY=
FIRECRAWL_API_KEY=
ANTHROPIC_API_KEY=

# Optional / defaults:
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022
DATABASE_URL=sqlite:///./agency_ai.db
# USE_JUNK_MODEL=true → junk-removal-seo | false → llama3.1:8b (default)
USE_JUNK_MODEL=false
OLLAMA_URL=http://localhost:11434
DEFAULT_NICHE=Junk Removal
SEARCH_RESULTS_LIMIT=5
